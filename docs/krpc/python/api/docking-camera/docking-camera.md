# DockingCamera

Camera service.

available
:   Check if the Camera API is available.

    Attribute:
    :   Read-only, cannot be set

    Return type:
    :   bool

static camera(*part*)
:   Get a Camera part.

    Parameters:
    :   **part** ([*SpaceCenter.Part*](../space-center/parts.md#SpaceCenter.Part "SpaceCenter.Part"))

    Return type:
    :   [`Camera`](./camera.md#DockingCamera.Camera "DockingCamera.Camera")
